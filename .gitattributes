# 文档配套示例工程走 Git LFS，避免 zip 把仓库历史撑大。
# 使用前安装 git-lfs，每个克隆执行一次：git lfs install
# 放入 public/downloads/<分类>/*.zip 后照常 add / commit / push。
public/downloads/*.zip filter=lfs diff=lfs merge=lfs -text
public/downloads/**/*.zip filter=lfs diff=lfs merge=lfs -text
