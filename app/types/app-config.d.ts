declare module "nuxt/schema" {
  interface AppConfigInput {
    seo?: {
      title?: string;
      titleTemplate?: string;
      description?: string;
    };
  }

  interface AppConfig {
    seo: {
      title: string;
      titleTemplate: string;
      description: string;
    };
  }
}

declare module "@nuxt/schema" {
  interface AppConfigInput {
    seo?: {
      title?: string;
      titleTemplate?: string;
      description?: string;
    };
  }

  interface AppConfig {
    seo: {
      title: string;
      titleTemplate: string;
      description: string;
    };
  }
}

export {};
