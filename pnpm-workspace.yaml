# 存在本文件时 pnpm 10+ 要求 packages 非空（CI 会 pnpm install --frozen-lockfile）
packages:
  - "."

# Docus 钉死 ~6.6.0；该版本 DevTools RPC 会超时。6.7.7+ 已改为 asEvent()。
overrides:
  nuxt-og-image: 6.7.8

allowBuilds:
  better-sqlite3: true
  esbuild: true
  vue-demi: true
