---
title: 故障排除
description: 使用 Docus 时常见的故障和解决方案。
navigation:
  icon: i-lucide-wrench
seo:
  description: 针对常见 Docus 问题的故障排除指南和解决方案。
---

## `pnpm` 问题

### 批准构建脚本

如果您在使用 `pnpm` 时遇到构建或开发错误，特别是与 `better-sqlite3` 依赖相关的错误，您可能需要批准某些包进行构建。

运行以下命令以批准包进行构建：

```bash [Terminal]
pnpm approve-builds
```

当出现提示时，从包列表中选择 `better-sqlite3` 和 `sharp` 以批准它们进行构建。

### 启用不规范的提升 (兼容模式)

如果您看到诸如 `Can't resolve 'tailwindcss'` 或 `Can't resolve '@nuxt/ui'` 等错误，您不一定需要导入它们，您可以直接应用扁平的 `node_modules` 布局（如 npm 或 yarn）。

您可以通过创建一个包含以下内容 `.npmrc` 文件来启用兼容模式：

```bash [.npmrc]
shamefully-hoist=true
```
