# Docus 个人技术博客/文档站 需求规格说明书

## 1\. 项目概述

### 1\.1 项目背景

替代 VitePress 搭建个人技术博客、知识库、笔记文档站，基于 **Nuxt4 \+ Docus \+ Nuxt Content** 实现，完全复刻 VitePress 经典视觉布局与写作体验，同时保留 Nuxt 生态高扩展性，兼顾极致 SEO、轻量化部署、无门槛 Markdown 写作，解决 Content Wind 样式简陋、原生 VitePress 扩展性不足的痛点。

### 1\.2 项目定位

- **核心用途**：个人技术笔记、博客文章、知识文档、学习归档

- **视觉标准**：完全对齐 VitePress 默认主题样式、布局、交互、排版

- **技术底座**：Nuxt4 \+ Docus v3 \+ Nuxt Content \+ Tailwind CSS 4 \+ MDC

- **运行模式**：默认 SSG 静态预渲染，极致 SEO，支持按需切换 SSR

- **核心优势**：VitePress 颜值与写作体验 \+ Nuxt 全栈扩展能力双向兼容

### 1\.3 整体目标

- 页面布局、排版、代码样式、交互效果**1:1 复刻 VitePress**

- 零配置开箱即用，专注 Markdown 写作，降低运维成本

- 全站 SEO 优化，支持百度、谷歌高效收录

- 适配移动端、桌面端，明暗主题无缝切换

- 保留扩展能力，后续可新增评论、统计、作品集等功能

## 2\. 整体架构与页面布局需求

### 2\.1 全局固定布局（对标 VitePress 经典三栏）

全站统一采用标准文档布局，结构固定，适配所有文档/博客页面：

1. **顶部导航栏（Navbar）**：固定置顶，包含站点名称、导航菜单、搜索入口、明暗模式切换、外部链接

2. **左侧侧边栏（Sidebar）**：自动读取 content 目录树，生成分类导航、页面分组、路由高亮，支持折叠展开

3. **中间主内容区**：文章正文、Markdown 渲染主体，固定舒适阅读宽度，排版规整

4. **右侧 TOC 目录栏**：自动抓取页面 H2\-H4 标题，滚动实时高亮锚点，悬浮固定

### 2\.2 自适应适配规则

- 桌面端（≥1280px）：三栏完整展示（左导航\+中正文\+右目录）

- 平板端（768px\-1280px）：隐藏右侧 TOC，保留左侧导航\+正文

- 移动端（\<768px）：左右侧边栏全部隐藏，顶部弹出抽屉式导航，仅展示正文内容

## 3\. 核心功能需求

### 3\.1 内容写作能力（对标 VitePress 全功能）

完全兼容 VitePress 写作语法，无需修改旧文档，无缝迁移：

- **基础 Markdown**：标题、段落、列表、表格、引用、链接、图片、分割线全支持

- **增强代码块**：Shiki 高亮、代码行号、一键复制、行高亮、diff 代码块、代码折叠

- **自定义提示容器**：支持 tip、warning、danger、info 彩色提示块

- **高级语法**：Mermaid 流程图、KaTeX 数学公式、内联目录 \[\[toc\]\]

- **Frontmatter 配置**：支持单页自定义标题、描述、排序、草稿状态、侧边栏显隐、封面图

标准 Frontmatter 规范：

```Plain Text
---
title: 文章标题
description: 页面描述（SEO 专用）
order: 1 // 侧边栏排序权重
draft: false // 是否为草稿（草稿不打包发布）
sidebar: true // 是否显示侧边栏
toc: true // 是否显示右侧目录
---
```

### 3\.2 导航与侧边栏功能

- **自动生成导航**：扫描 content 目录文件夹结构，自动生成侧边栏菜单，无需手动配置路由

- **分组折叠**：支持文件夹分组，可折叠/展开分类目录

- **路由高亮**：当前浏览页面侧边栏菜单自动高亮

- **自定义排序**：通过 frontmatter order 字段自由调整菜单顺序

- **页面单独控制**：支持单页关闭侧边栏、关闭 TOC 目录

### 3\.3 全站搜索功能

- 内置离线全文搜索，无需后端接口，对标 VitePress 内置搜索体验

- 支持标题、正文、描述模糊检索，搜索结果高亮关键词

- 忽略草稿页面，仅检索正式发布内容

- 支持快捷键唤起搜索弹窗

### 3\.4 主题与样式功能

- **双主题切换**：明亮模式/暗黑模式一键切换，记忆用户偏好，系统跟随适配

- **样式复刻**：1:1 对齐 VitePress 默认配色、圆角、阴影、行高、字体、间距

- **代码块样式统一**：默认 VitePress 同款高亮主题、配色、圆角、内边距

- **平滑交互**：页面跳转、主题切换、菜单展开收起平滑过渡动画

### 3\.5 SEO 能力（核心刚需）

- 全站 SSG 静态预渲染，输出完整 HTML，爬虫零障碍抓取

- 自动生成 sitemap\.xml、robots\.txt、规范链接

- 自动适配 OG 社交预览标签、页面结构化数据

- 每篇文章支持自定义 SEO 标题、描述，无重复元信息

- 自动生成 llms\.txt 适配 AI 检索场景，提升内容曝光

### 3\.6 扩展能力（预留）

保留 Nuxt 原生扩展能力，后续可无缝新增功能，无需重构项目：

- 接入评论系统（Waline/Giscus）

- 新增文章归档、标签、分类、阅读统计

- 添加个人作品集、关于页面、访客统计

- 支持多语言国际化部署

## 4\. 样式与视觉规范（VitePress 复刻标准）

### 4\.1 全局排版规范

- 默认字体：系统无衬线字体，中英文适配最优阅读体验

- 正文行高：1\.75，段落间距宽松舒适，对标 VitePress 阅读质感

- 标题层级：H1\-H6 字号、粗细、边距完全对齐原版，二级标题自带分割线

- 留白规范：页面左右、上下留白统一，主内容区固定最大宽度，避免过宽阅读疲劳

### 4\.2 核心组件样式规范

- **代码块**：圆角、阴影、内边距、行高、高亮配色与 VitePress 一致，自带复制按钮

- **引用块**：左侧彩色边框、浅色背景、缩进间距统一

- **列表/表格**：边框、间距、hover 效果复刻原版

- **侧边栏**：菜单 hover 高亮、激活态配色、折叠动画完全一致

- **TOC 目录**：滚动跟随、当前章节高亮、锚点跳转平滑

## 5\. 项目目录规范

遵循 Docus 官方标准目录结构，简洁易维护，适配自动化部署：

```Plain Text
docus-blog/
├── content/                # 所有博客/文档 Markdown 存放目录（核心写作目录）
├── components/             # 自定义组件（覆盖默认样式、新增功能）
├── layouts/                # 全局布局覆盖（复刻VitePress布局）
├── public/                 # 静态资源、图标、robots.txt
├── nuxt.config.ts          # 全局配置（导航、主题、SEO、模块）
├── mdc.config.ts           # MDC语法、代码高亮配置
└── tailwind.config.ts      # 主题配色、样式覆盖配置
```

## 6\. 部署与运维需求

### 6\.1 构建模式

- 开发环境：npm run dev（热更新、实时预览）

- 生产构建：npm run generate（全量 SSG 静态打包，无服务依赖）

### 6\.2 部署平台适配

支持所有静态平台一键部署，零配置：Vercel、Netlify、GitHub Pages、阿里云OSS、腾讯云COS

### 6\.3 运维特性

- 增量构建，打包速度快，百篇文章秒级构建

- 草稿页面自动过滤，不参与打包发布

- 自动压缩图片、代码、样式，页面加载速度极致优化

- 支持 Git 提交自动部署，写完推送即更新站点

## 7\. 约束与兼容要求

- **语法兼容**：100% 兼容 VitePress Markdown 语法，支持旧文档无缝迁移

- **浏览器兼容**：兼容主流现代浏览器，移动端适配无错位、无样式塌陷

- **性能约束**：首页、文章页 LCP 优秀，无冗余 JS、CSS，PageSpeed 高分

- **维护约束**：尽量使用官方原生能力，减少自定义冗余代码，便于后续版本升级

## 8\. 验收标准

1. 页面布局、视觉样式、交互效果与 VitePress 默认主题无明显差异

2. 所有 VitePress 常用写作语法正常渲染，代码块、提示块、图表功能可用

3. 三栏布局自适应正常，移动端抽屉导航流畅可用

4. 全站静态打包成功，无报错，SEO 文件自动生成完整

5. 明暗主题切换正常，用户偏好记忆生效

6. 全文搜索功能正常，关键词检索精准

7. 可在任意静态平台正常部署、访问，页面加载流畅无报错

> （注：部分内容可能由 AI 生成）
